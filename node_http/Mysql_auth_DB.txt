create table users (
id int not null AUTO_INCREMENT,
authId varchar(50) not null,
username varchar(30),
password varchar(255),
salt varchar(255),
displayName varchar(50),
primary key(id),
unique (authId)
) ENGINE =InnoDB;